{
  "name": "gnotes",
  "version": "1.0.0",
  "description": "Organize notes on things you have read",
  "main": "index.js",
  "config": {
    "distDir": "docs"
  },
  "scripts": {
    "new": "node scripts/new.js",
    "build": "node scripts/build.js",
    "view": "npm run build && http-server -o $npm_package_config_distDir",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Claire Froelich",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "10.0.0",
    "gray-matter": "4.0.3",
    "http-server": "14.0.0",
    "marked": "4.0.8",
    "pug": "3.0.2"
  }
}
